apiVersion: v1
kind: Service
metadata:
  name: postgres-db
  labels:
    app: postgres-db
spec:
  selector:
    app: postgres-db
  ports:
  - name: postgres
    port: 5432
    targetPort: postgres
  type: ClusterIP   # Accessible only inside the cluster