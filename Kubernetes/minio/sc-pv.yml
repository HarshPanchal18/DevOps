--- # StorageClass

apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: sc-minio
  namespace: minio-operator

provisioner: kubernetes.io/no-provisioner
reclaimPolicy: Retain
volumeBindingMode: WaitForFirstConsumer

--- # PersistentVolumes (1..6)

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-1
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio1
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker

---

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-2
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio2
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker

---

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-3
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio3
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker

---

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-4
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio4
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker

---

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-5
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio5
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker

---

apiVersion: v1
kind: PersistentVolume

metadata:
  name: minio-pv-6
  namespace: minio-operator
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc-minio
  hostPath:
    path: /var/data/minio6
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kube-cluster-worker